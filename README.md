# mint-rdp-kiosk
Ansible playbook that turns old PCs into passwordless Linux Mint kiosk terminals auto-launching an RDP session to a Windows host — with remote admin access, auto-updates, and an optional Semaphore UI web interface.
